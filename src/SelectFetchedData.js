import React, { useState } from "react";
import Data from "./Data";

function SelectFetchedData(props) {
  const [isSelected, setIsSelected] = useState(null);
  return <div></div>;
}

export default SelectFetchedData;
